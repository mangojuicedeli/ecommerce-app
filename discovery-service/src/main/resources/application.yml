server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false # 운영시 삭제 필요
    eviction-interval-timer-in-ms: 3000 # 하트비트 수신 점검

